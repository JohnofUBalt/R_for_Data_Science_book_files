library(tidyverse)
library(nycflights13)

# see some of the table with glimpse
glimpse(flights)

#see whole table (opens new window) with View
View(flights)

#see all columns with print, width
print(flights, width = Inf)
